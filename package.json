{
  "scripts": {
    "edit": "nvim resume.md",
    "start": "md-to-pdf --config-file resume.config.json --watch resume.md",
    "build": "md-to-pdf --config-file resume.config.json resume.md",
    "config": "vim resume.config.json"
  },
	"dependencies": {
		"md-to-pdf": "^5.2.4"
	}
}
