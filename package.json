{
  "scripts": {
    "config": "vim resume.config.json",
    "build": "md-to-pdf --config-file resume.config.json resume.md",
    "watch": "md-to-pdf --config-file resume.config.json --watch resume.md"
  },
	"dependencies": {
		"md-to-pdf": "^5.2.4"
	}
}
